{
  "name": "renting-together-platform",
  "version": "1.0.0",
  "description": "Flat listings, click tracking, and community announcements for Renting Together",
  "type": "module",
  "scripts": {
    "dev": "npm run generate:listings && vite",
    "build": "npm run generate:listings && vite build",
    "generate:listings": "node scripts/generate-listing-pages.js",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^6.2.0"
  }
}
